name: hosted-staging-proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  proof:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run hosted staging proof
        shell: pwsh
        env:
          STAGING_BASE_URL: ${{ secrets.STAGING_BASE_URL }}
        run: ./scripts/verify-hosted-staging.ps1
