{
  "name": "@cloud-superbrain/cloudflare-llm-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
