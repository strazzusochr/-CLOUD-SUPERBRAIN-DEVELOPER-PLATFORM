# 01 Home - Visual Target

Route: `/home`

Goal: calm product start for the Cloud Superbrain Developer Platform. It must look like the industrial blueprint, not a project-status wall, and must show a glowing cortex/brain hero as DEV-ONLY visual organism.

Required layout:
- Header and primary Workbench entry.
- Glowing 3D/pseudo-3D cortex hero with explicit `DEV-ONLY` and `fake_stats=false`.
- Product-mode shortcuts for Games, Apps, Media, Docs.
- Product constants may be shown, but no fake live metrics, no external-gate matrix, no recovery history.

Element rules:
- Cortex proof button must produce visible `PASS home_hero_check`.
- Workbench/Organism/Evidence links are same-origin navigation only.
- Cortex visual is read-only decoration unless an explicit action proves local state.
- No provider write, no live LLM call, no production claim.
